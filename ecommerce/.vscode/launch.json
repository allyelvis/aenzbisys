{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "chrome",
      "request": "launch",
      "name": "Launch Chrome against ecommerce",
      "url": "http://localhost:4200",
      "webRoot": "${workspaceFolder}"
    }
  ]
}